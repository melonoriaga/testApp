import * as React from 'react'

import HomeContainer from '../../components/Home/HomeContainer'

export default function HomeScreen({ navigation }) {
    return <HomeContainer handleNavigation={navigation} />
}
